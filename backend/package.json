{
  "name": "hack4krak-backend",
  "private": true,
  "scripts": {
    "install": "cargo fetch",
    "build": "cargo build",
    "dev": "cargo run",
    "lint": "cargo fmt -- --check && cargo clippy",
    "lint:fix": "cargo fmt && cargo clippy --fix",
    "test": "cargo test"
  }
}
