[package]
name = "openapi-cli"
edition = "2024"
version.workspace = true
publish = false

[dependencies]
tokio = { version = "1.44.2", features = ["fs", "macros", "rt", "rt-multi-thread"] }
hack4krak-backend = { path = "../" }
